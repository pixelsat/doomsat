[package]
name = "shared"
version = "0.1.0"
edition = "2024"

[features]
default = ["serde"]
stm = []
client = []
serde = ["dep:rkyv"]

[dependencies]
rkyv = { version = "0.8.18", features = ["little_endian"], optional = true }

[build-dependencies]
bindgen = "0.72.1"